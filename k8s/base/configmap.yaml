apiVersion: v1
kind: ConfigMap
metadata:
  name: discount-service-config
  namespace: default
data:
  DISCOUNT.MAX_APPLIED: "10"
  DB.HOST: "discount-db-0.discount-db.dev.svc.cluster.local"
  DB.NAME: "discount"
  DB.USER: "postgres"
  DB.PASSWORD: "postgres"
